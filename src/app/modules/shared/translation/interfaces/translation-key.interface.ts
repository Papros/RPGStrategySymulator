export interface ITranslationKeys {
    rootPage: {
        footer: {
            msg: string;
        }
    }
    loginPage: {
        title: string,
        prompt: string,
        player: string,
        gamemaster: string,
    }
};
